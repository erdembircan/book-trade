const config = {
  serverPort: 3000,
  sessionSecret: 'sessionsecret',
  mongoDb: 'mongodb://127.0.0.1:27017/book_club',
  jwtSecret: 'not definedyetbutworkingonit',
  sha256Secret: 'ikikişininbildiğisırsırdeğildir',
};

// export default config;
module.exports = config;
